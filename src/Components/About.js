import React from 'react'

function About() {
    return (
        <div>
            <h1>A WebSite Demo ReactJs( Hooks ) + Spring Java (Spring Boot + Spring Data Jpa ) + Rest API</h1>
        </div>
    )
}

export default About
